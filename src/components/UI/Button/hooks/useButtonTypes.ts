
function useButtonTypes() {
  const styles = {
    'primary': 'bg-primary text-white hover:bg-secondary focus:ring-secondary',
    'primaryOutline': 'text-primary border-primary bg-white hover:bg-gray-100 focus:ring-secondary',
  }

  return styles;
}

export default useButtonTypes;